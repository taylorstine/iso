import {Map, fromJS} from 'immutable'
//import workouts from './workouts-reducer'

export default (state, action) =>{
  return new Map({
    //workouts: workouts(state.get('workouts'), action),
  })
};